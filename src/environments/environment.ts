// Exportation de la constante "environment" qui est un objet JavaScript
export const environment = {
    // La propriété "production" indique si l'application est en mode de production
    // Si cette propriété est définie à "false", cela signifie que l'application est en mode de développement
    production: false,

    // La propriété "api_url" contient l'URL de l'API avec laquelle l'application communique
    // Dans ce cas, l'URL de l'API est "http://localhost:8080/api"
    api_url: 'http://localhost:8080/api',
};
